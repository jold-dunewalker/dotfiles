- defaults:
    link:
      relink: true

- shell:
  - [git submodule update --init --recursive, Installing submodules]

- clean: ['~']

- link:
    ~/.profile: profile
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc
    ~/.terminator.config: terminator.config
