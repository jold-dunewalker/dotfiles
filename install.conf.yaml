- defaults:
    link:
      relink: true

- shell:
  - [git submodule update --init --recursive, Installing submodules]

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.terminator.config: terminator.config
